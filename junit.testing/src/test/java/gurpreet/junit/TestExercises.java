package gurpreet.junit;

//import static org.junit.Assert.assertArrayEquals;
//import static org.junit.Assert.assertTrue;
//import static org.junit.jupiter.api.Assertions.assertTrue;

import gurpreet.junit.exercises.FlipBooleans;

import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;
/**
 * Unit test for simple App.
 */
public class TestExercises
{
    /**
     * Rigorous Test :-)
     */
    @Test
    public void shouldAnswerWithTrue()
    {
       // assertTrue( true );
       // Assertions.assertThat(true).isTrue();
    }
    @Test
    public void FlipBooleans(){
     //Assertions.assertThat(  FlipBooleans.tryInput(new boolean[]{ true, false, false, true}, new boolean[]{ false, true, true, false})).isTrue();
   // Assertions.assertThat(FlipBooleans.tryInput(new boolean[]{ false, false, false, false, false}, new boolean[]{ true, true, true, true, true})).isTrue();

    }


}
